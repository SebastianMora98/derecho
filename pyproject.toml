[project]
name = "ema-documentos"
version = "0.1.0"
description = "Convierte libros y documentos a Markdown y los prepara para estudio asistido por IA"
requires-python = ">=3.10,<3.14"
dependencies = [
    "markitdown[pdf,docx,pptx,xlsx,outlook]>=0.1.1",
]

[tool.uv]
package = false
